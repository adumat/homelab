NUT_MONITOR_PASSWORD: bws://07518fe9-65db-4960-95cc-b3f00157d8fa
# Consumed by patches/controller/cluster.yaml for the kube-apiserver OIDC issuer.
# Built from the existing base-domain item so the domain never appears in a
# tracked file. bws-inject substitutes bws:// references inline, so the "auth."
# prefix survives. This template is the source of truth: editing the generated
# talenv.secrets.yaml directly is silently undone by `just gen-config`.
AUTH_DOMAIN: auth.bws://88234b63-9f5f-4a52-88ba-b42b00fde831
