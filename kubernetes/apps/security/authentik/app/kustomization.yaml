---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# httproute.yaml and referencegrant.yaml are gone: auth.${DOMAIN} now belongs to
# authelia, and the SecurityPolicy grant was reissued for the authelia Service.
# The rest stays deployed but unrouted for the rollback window - do NOT delete
# this app or its database until that window closes (Task 15).
resources:
  - ./externalsecret.yaml
  - ./helmrelease.yaml
  - ./ocirepository.yaml
  - ./blueprints
