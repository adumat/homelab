worker_processes 1;
events { worker_connections 256; }
http {
  include /etc/nginx/mime.types;
  server {
    listen 8080;
    root /www;
    index index.html;
    location / { try_files $uri /index.html; }
  }
}
