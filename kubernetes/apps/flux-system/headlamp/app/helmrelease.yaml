---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: headlamp
spec:
  interval: 1h
  chart:
    spec:
      chart: headlamp
      version: 0.43.0
      sourceRef:
        kind: HelmRepository
        name: headlamp
  values:
    config:
      watchPlugins: true
      oidc:
        secret:
          create: false
        externalSecret:
          enabled: true
          name: headlamp-oidc
        callbackURL: "https://gitops.${DOMAIN}/oidc-callback"
        # MUST be set here, not in the secret. With the externalSecret path the
        # chart only renders -oidc-client-id, -oidc-client-secret,
        # -oidc-idp-issuer-url and -oidc-callback-url; there is no -oidc-scopes
        # argument, so OIDC_SCOPES in the secret is silently ignored and Headlamp
        # falls back to its default "openid profile email".
        #
        # That means no groups claim, so kube-apiserver authenticates the user as
        # https://<issuer>#matteo with no group memberships and RBAC matches
        # nothing against oidc-headlamp-cluster-admin:
        #   nodes.metrics.k8s.io is forbidden: User "https://auth.../#matteo"
        #   cannot list resource "nodes" ... at the cluster scope
        # Authentik emitted groups regardless of scope, which hid this.
        scopes: "openid,profile,email,groups,offline_access"
    pluginsManager:
      enabled: true
      configContent: |
        plugins:
          - name: flux
            source: https://artifacthub.io/packages/headlamp/headlamp-plugins/headlamp_flux
            version: 0.5.0
          - name: keda
            source: https://artifacthub.io/packages/headlamp/headlamp-plugins/headlamp_keda
            version: 0.1.1-beta
          - name: cert-manager
            source: https://artifacthub.io/packages/headlamp/headlamp-plugins/headlamp_cert-manager
            version: 0.1.0
          - name: rook-ceph
            source: https://artifacthub.io/packages/headlamp/headlamp-rook/headlamp-rook-plugin
            version: 0.2.6
          - name: envoy-gateway
            source: https://artifacthub.io/packages/headlamp/headlamp-envoy-gateway/envoy-gateway
            version: 0.3.0
          - name: external-secrets
            source: https://artifacthub.io/packages/headlamp/external-secrets-operator-headlamp-plugin/external-secrets-operator
            version: 0.1.0-beta7
    httpRoute:
      enabled: true
      parentRefs:
        - name: envoy-internal
          namespace: network
          sectionName: https
      hostnames:
        - gitops.${DOMAIN}
      annotations:
        gethomepage.dev/enabled: "true"
        gethomepage.dev/group: Services
        gethomepage.dev/name: Headlamp
        gethomepage.dev/icon: headlamp.png
        gethomepage.dev/description: Kubernetes Dashboard
    resources:
      requests:
        cpu: 50m
        memory: 64Mi
      limits:
        memory: 256Mi
