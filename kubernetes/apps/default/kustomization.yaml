---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
components:
  - ../../components/common
# No resources: the echo test route was the only app here and was removed
# 2026-07-30. The common component still applies the namespace, alerts, shared
# repos and sops secret. This directory can go entirely if nothing is ever
# deployed to the default namespace.
